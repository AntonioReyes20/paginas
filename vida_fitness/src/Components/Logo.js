import React from 'react';
import './Estilos.css';

const Logo = (config) => {
    return (
        <div>
            <img src={'Pictures/logo.png'} className={'Logo'}/>
        </div>
    );
};

export default Logo;